import 'package:flutter/material.dart';

class AIPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AI'),
      ),
      body: Center(
        child: Text('This is the AI page', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
